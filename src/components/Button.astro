
---
const {url, external, title} = Astro.props;
import {Icon} from "astro-icon";
---


<a href={url} target={external ? "_blank": undefined} class="">
    {title}
    {
        external ? (
            <Icon >
        ) : ("")
    }
</a>