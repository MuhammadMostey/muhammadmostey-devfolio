---
const {tagName, css} = Astro.props;




export interface Props {
  tagName: string;
  css ?: string;
 
}


---


<div 
class=`inline-block bg-opacity-40 rounded-xl ${css} px-1 text-sm`>
    <div>{tagName}</div> 
</div>

